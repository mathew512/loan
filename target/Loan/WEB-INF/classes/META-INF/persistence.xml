<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             version="3.0">

    <persistence-unit name="loanPU" transaction-type="RESOURCE_LOCAL">

        <!-- ================= ENTITIES ================= -->
        <class>app.model.Loan</class>
        <class>app.model.Customer</class>
        <class>app.model.Repayment</class>
        <class>app.model.LoanType</class>
        <class>app.model.User</class>

        <properties>

            <!-- ================= DB CONNECTION ================= -->
            <property name="jakarta.persistence.jdbc.driver"
                      value="org.postgresql.Driver"/>

            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:postgresql://localhost:5432/loan_db"/>

            <property name="jakarta.persistence.jdbc.user"
                      value="postgres"/>

            <property name="jakarta.persistence.jdbc.password"
                      value="postgres"/>

            <!-- ================= HIBERNATE ================= -->
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.PostgreSQLDialect"/>

            <property name="hibernate.hbm2ddl.auto"
                      value="update"/>

            <property name="hibernate.show_sql"
                      value="true"/>

            <property name="hibernate.format_sql"
                      value="true"/>

        </properties>

    </persistence-unit>

</persistence>